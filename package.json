{
  "name": "kayal_maam_resume_website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS='--openssl-legacy-provider' next dev",
    "build": "cross-env NODE_OPTIONS='--openssl-legacy-provider' next build && next export",
    "deploy": "cross-env NODE_OPTIONS='--openssl-legacy-provider' next build && next export && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages",
    "start": "cross-env NODE_OPTIONS='--openssl-legacy-provider' next start",
    "lint": "cross-env NODE_OPTIONS='--openssl-legacy-provider' next lint"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "aos": "^2.3.4",
    "cross-env": "^7.0.3",
    "next": "11.0.1",
    "react": "17.0.2",
    "react-bootstrap": "^1.6.1",
    "react-countup": "^4.4.0",
    "react-dom": "17.0.2",
    "react-loader-spinner": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "7.29.0",
    "eslint-config-next": "11.0.1"
  },
  "engines": {
    "node": "^18"
  }
}
